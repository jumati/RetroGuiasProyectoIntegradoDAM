<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetroGuias" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RetroGuias" parent="Base.Theme.RetroGuias" />

    <style name="intro" parent="Theme.RetroGuias">
        <item name="android:windowBackground">@drawable/intro</item>
    </style>
</resources>